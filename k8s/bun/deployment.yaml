apiVersion: apps/v1
kind: Deployment
metadata:
  name: bun-app-deployment
spec:
  replicas: 3  # Increase replicas
  selector:
    matchLabels:
      app: bun-app
  template:
    metadata:
      labels:
        app: bun-app
    spec:
      containers:
      - name: bun-app
        image: goodeesh/my-bun-app:local
        imagePullPolicy: Never
        resources:
          requests:
            cpu: "1000m"
            memory: "256Mi"  # Increase memory request
          limits:
            cpu: "2000m"
            memory: "512Mi"    # Significantly increase memory limit
        ports:
        - containerPort: 5000
        startupProbe:        # Add startup probe
          httpGet:
            path: /health
            port: 5000
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:       # Add liveness probe
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20